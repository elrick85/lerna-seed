{
  "name": "root",
  "private": true,
  "scripts": {
    "new-version": "lerna version prerelease --preid SNAPSHOT --yes --exact",
    "new-patch": "lerna version prepatch --preid SNAPSHOT --yes --exact",
    "publish-canary": "lerna publish --canary --yes --",
    "diff": "lerna diff",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.10.7",
    "rimraf": "^2.6.3",
    "typescript": "^3.3.1"
  }
}
