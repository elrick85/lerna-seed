{
  "name": "root",
  "private": true,
  "scripts": {
    "new-version": "lerna version prerelease --preid SNAPSHOT --yes --exact --no-push",
    "new-prepatch": "lerna version prepatch --preid SNAPSHOT --yes --exact --no-push",
    "new-preminor": "lerna version preminor --preid SNAPSHOT --yes --exact --no-push",
    "new-premajor": "lerna version premajor --preid SNAPSHOT --yes --exact --no-push",
    "new-patch": "lerna version patch --yes --exact --no-push",
    "new-minor": "lerna version minor --yes --exact --no-push",
    "new-major": "lerna version major --yes --exact --no-push",
    "publish": "lerna publish from-package --yes --exact",
    "publish-release": "lerna publish from-git --yes --release --exact",
    "diff": "lerna diff",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.10.7",
    "rimraf": "^2.6.3",
    "typescript": "^3.3.1"
  }
}
