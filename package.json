{
  "name": "root",
  "private": true,
  "scripts": {
    "commit": "lerna version --no-push --yes",
    "new-version": "lerna version prepatch --preid SNAPSHOT --no-push --yes",
    "diff": "lerna diff",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.10.7",
    "rimraf": "^2.6.3",
    "typescript": "^3.3.1"
  }
}
