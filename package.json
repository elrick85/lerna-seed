{
  "name": "root",
  "private": true,
  "scripts": {
    "new-version": "lerna version release--no-push --yes --exact",
    "new-preminor": "lerna version preminor --preid SNAPSHOT --no-push --yes --exact",
    "diff": "lerna diff",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.10.7",
    "rimraf": "^2.6.3",
    "typescript": "^3.3.1"
  }
}
